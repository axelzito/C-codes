#include <iostream>
#include <algorithm>
#include <string>

using namespace std;

int main(){

    int t;

    cin>>t;

    for(int z=0; z<t; z++){

        int n, l, c;
        string pal;
        int cont = 0;

        // n - palavras, l - linhas p pag, c - caracteres p linha

        cin >> n >> l >> c;
        cin.ignore();
        getline(cin, pal);
        string aux;
        for(int i = 0; i<n; i++){

        }
    }

    return 0;
}
